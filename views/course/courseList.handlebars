<table class="table">
    <thead>
        <tr>
            <th scope="col">Title</th>
            <th scope="col">Description</th>
            <th scope="col">Category</th>
            <th scope="col">Imagem</th>
            <th scope="col">Owner id</th>
            <th scope="col">Status</th>
            {{#if admin}}
            <th scope="col">Editar</th>
            <th scope="col">Deletar</th>
            {{/if}}
        </tr>
    </thead>
    <tbody>
        {{#each courses}}
        <tr>
            <th scope="row">{{title}}</th>
            <th scope="row">{{description}}</th>
            <th scope="row">{{category}}</th>
            <td>
            <image width="50px" height="50px" src="uploads/{{imagem}}">
            </td>
             <th scope="row">{{owner_id}}</th>
              <th scope="row">{{status}}</th>
            {{#if ../admin}}
            <td><a href="/telaUpdateCourse/{{id}}">Editar</a></td>
            <td><a href="/courses/delete/{{id}}">Deletar</a></td>
            {{/if}}
        </tr>
        {{/each}}
    </tbody>
</table>



{{!-- <table border="1px">
    <tr>
        <td> Title </td>
        <td> Description </td>
        <td> Category </td>
        <td> Owner id </td>
        <td> Status </td>
        <td> Editar </td>
        <td> Deletar </td>
    </tr>
    {{#each courses}}
    <tr>
        <td>{{title}}</td>
        <td>{{description}}</td>
        <td>{{category}}</td>
        <td>{{owner_id}}</td>
        <td>{{status}}</td>
        <td><a href="telaUpdateCourse/{{id}}">Editar</a></td>
        <td><a href="courses/delete/{{id}}">Deletar</a></td>
    </tr>
    {{/each}}
</table> --}}